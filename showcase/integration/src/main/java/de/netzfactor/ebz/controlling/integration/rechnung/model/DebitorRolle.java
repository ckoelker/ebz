package de.netzfactor.ebz.controlling.integration.rechnung.model;

/** Debitor-Typ: Firmen- vs. Privatkunde (steuert u. a. den Pflicht-Empfang der E-Rechnung, R2). */
public enum DebitorRolle {
    FIRMA, PRIVAT
}
