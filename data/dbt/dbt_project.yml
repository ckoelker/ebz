name: ebz_controlling
version: 1.0.0
config-version: 2
profile: ebz_controlling
model-paths:
  - models
seed-paths:
  - seeds
macro-paths:
  - macros
test-paths:
  - tests
target-path: target
clean-targets:
  - target
  - dbt_packages
  - logs
vars:
  currency: EUR
  vat_rate: 0.19
  forecast_anchor: ''
models:
  ebz_controlling:
    staging:
      +materialized: view
    marts:
      +materialized: table
